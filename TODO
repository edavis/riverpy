# -*- mode: org -*-

- Backend:
  - Add option to generate JSON and/or JSONP
  - Add "special" rivers:
    - "Latest" would be everything from the last run
    - "Firehose" would be everything from all rivers
  - Write log output to file
  - Only update a given category with URL#name
- Frontend:
  - Use permalinks to link directly to a river
  - Display spinner/progress bar when changing rivers
  - Have navigation dropdown float at top
  - Make it look better on mobile
- Subscription Lists:
  - Add metadata to categories/feeds
    - position for categories
    - different weights for feeds
    - custom names for feeds
  - Add ability to "include" other subscription lists
- Docs:
  - Add a ChangeLog
